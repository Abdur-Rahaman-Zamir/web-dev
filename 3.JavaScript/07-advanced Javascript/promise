let complete = true;

let prom = new Promise(function (resolve, reject) {
    if (complete) {
        resolve("Task completed");
    } else {
        reject("Task not completed");
    }
});

console.log(prom);
